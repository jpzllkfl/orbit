# TrueNAS / Dockge — Windows T: = \\192.168.1.177\media
# Host path on this NAS: /mnt/broken_eye/media (NOT /mnt/tank/media)

services:
  orbit:
    build: .
    image: orbit:latest
    container_name: orbit
    ports:
      - "${ORBIT_PORT:-8090}:8090"
    environment:
      PORT: 8090
      ORBIT_DOCKER: "1"
      ORBIT_OMS_AUTO_SEED: "1"
      ORBIT_MEDIA_ROOT: ${ORBIT_MEDIA_ROOT:-/mnt/broken_eye/media}
      # Free TMDB v3 key — artwork/metadata work out of the box (like Plex). Get one at themoviedb.org/settings/api
      ORBIT_TMDB_API_KEY: ${ORBIT_TMDB_API_KEY:-}
    user: "0:0"
    entrypoint:
      - /bin/sh
      - -c
      - mkdir -p /app/server/data/states && chmod -R 777 /app/server/data && exec node server/index.js
    volumes:
      - ./data:/app/server/data
      # --- TrueNAS media share (\\192.168.1.177\media) ---
      - ${ORBIT_MEDIA_ROOT:-/mnt/broken_eye/media}/Anime:/media/anime:ro
      - ${ORBIT_MEDIA_ROOT:-/mnt/broken_eye/media}/Comedy:/media/comedy:ro
      - ${ORBIT_MEDIA_ROOT:-/mnt/broken_eye/media}/Documentaries:/media/documentaries:ro
      - "${ORBIT_MEDIA_ROOT:-/mnt/broken_eye/media}/Kids Movies:/media/kids-movies:ro"
      - "${ORBIT_MEDIA_ROOT:-/mnt/broken_eye/media}/Kids TV:/media/kids-tv:ro"
      - ${ORBIT_MEDIA_ROOT:-/mnt/broken_eye/media}/movies:/media/movies:ro
      - ${ORBIT_MEDIA_ROOT:-/mnt/broken_eye/media}/tv:/media/tv:ro
      - ${ORBIT_MEDIA_ROOT:-/mnt/broken_eye/media}/remote:/media/remote:ro
      - ${ORBIT_MEDIA_ROOT:-/mnt/broken_eye/media}/remote_d:/media/remote-d:ro
      - ${ORBIT_MEDIA_ROOT:-/mnt/broken_eye/media}/remote_E:/media/remote-e:ro
      - ${ORBIT_MEDIA_ROOT:-/mnt/broken_eye/media}/remote_I:/media/remote-i:ro
      - ${ORBIT_MEDIA_ROOT:-/mnt/broken_eye/media}/remote_J:/media/remote-j:ro
      - ${ORBIT_MEDIA_ROOT:-/mnt/broken_eye/media}/remote_L:/media/remote-l:ro
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8090/api/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s
